--------------------------------------------------------------------------------
-- Add required packages
--------------------------------------------------------------------------------
with <package>;
with Test_Data;
with Text_IO;

package body <stub_package> is

   ---------------------------------------------------------------------------
   -- Provided operations
   ---------------------------------------------------------------------------
   procedure op1 is separate;

   procedure op2 is separate;

   procedure op3 is separate;

   function op4
     (<in_param1> : <in_param_types>)
       return <return_type>
   is separate;

   --etc.

end Discrete_Iface;
